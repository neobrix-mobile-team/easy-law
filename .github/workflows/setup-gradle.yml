name: 🔧 공통 Gradle 환경 설정

on:
  workflow_call:
    inputs:
      setup-api-keys:
        description: 'GEMINI_API_KEY, LAW_API_KEY 설정 여부'
        required: false
        type: boolean
        default: false
    secrets:
      GOOGLE_SERVICES_JSON:
        required: true
      GEMINI_API_KEY:
        required: false
      LAW_API_KEY:
        required: false

jobs:
  setup:
    runs-on: ubuntu-latest
    name: ⚙️ 환경 설정
    steps:
      - name: 📥 코드 받아오기
        uses: actions/checkout@v4

      - name: ☕ JDK 17 설정
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle
          cache-dependency-path: |
            **/gradle.properties
            **/build.gradle

      - name: 🔧 Gradle wrapper 권한 설정
        run: chmod +x ./gradlew

      - name: 📝 local.properties 생성
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          LAW_API_KEY: ${{ secrets.LAW_API_KEY }}
        run: |
          # 기본 설정
          touch local.properties
          echo "sdk.dir=$ANDROID_HOME" >> local.properties
          
          # API 키 설정 (선택적)
          if [ "${{ inputs.setup-api-keys }}" = "true" ]; then
            if [ -n "$GEMINI_API_KEY" ]; then
              echo "GEMINI_API_KEY=$GEMINI_API_KEY" >> local.properties
            else
              echo "⚠️ GEMINI_API_KEY secret is missing!"
            fi
            
            if [ -n "$LAW_API_KEY" ]; then
              echo "LAW_API_KEY=$LAW_API_KEY" >> local.properties
            else
              echo "⚠️ LAW_API_KEY secret is missing!"
            fi
          fi

      - name: 🔥 google-services.json 생성
        env:
          G_SERVICES: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: |
          if [ -n "$G_SERVICES" ]; then
            echo "$G_SERVICES" | base64 -d > app/google-services.json
          else
            echo "⚠️ GOOGLE_SERVICES_JSON secret is missing!"
          fi

