name: 🔐 코드 품질 & 보안 검사

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  lint-and-detekt:
    name: 🔍 Lint & Detekt 검사
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 📥 코드 받아오기
        uses: actions/checkout@v4

      - name: ☕ JDK 17 설정
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle
          cache-dependency-path: |
            **/gradle.properties
            **/build.gradle

      - name: 🔧 Gradle wrapper 권한 설정
        run: chmod +x ./gradlew

      - name: 📝 local.properties 생성
        run: echo "sdk.dir=$ANDROID_HOME" >> local.properties

      - name: 🔥 google-services.json 생성
        run: echo '${{ secrets.GOOGLE_SERVICES_JSON }}' | base64 --decode > app/google-services.json

      - name: 🔍 Android Lint + Detekt 검사
        run: |
          ./gradlew lint detekt \
            --parallel \
            --build-cache \
            --no-daemon \
            --configure-on-demand \
            -Dorg.gradle.workers.max=8

      - name: 📤 Lint 결과 업로드
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lint-report
          path: |
            app/build/reports/lint-results*.html
            app/build/reports/lint-results*.xml
          retention-days: 7

      - name: 📤 Detekt 리포트 업로드
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: detekt-report
          path: |
            build/reports/detekt/
          retention-days: 7

  security-scan:
    name: 🔒 의존성 보안 취약점 검사
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # main/develop에만 실행 (PR에서는 스킵하여 시간 절약)
    if: github.event_name == 'push'

    steps:
      - name: 📥 코드 받아오기
        uses: actions/checkout@v4

      - name: ☕ JDK 17 설정
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle
          cache-dependency-path: |
            **/gradle.properties
            **/build.gradle

      - name: 🔧 Gradle wrapper 권한 설정
        run: chmod +x ./gradlew

      - name: 📝 local.properties 생성
        run: echo "sdk.dir=$ANDROID_HOME" >> local.properties

      - name: 🔥 google-services.json 생성
        run: echo '${{ secrets.GOOGLE_SERVICES_JSON }}' | base64 --decode > app/google-services.json

      - name: 🔒 OWASP Dependency Check 실행
        run: |
          ./gradlew dependencyCheckAnalyze \
            --parallel \
            --build-cache \
            --no-daemon \
            -Dorg.gradle.workers.max=8 || true

      - name: 📤 보안 검사 결과 업로드
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-scan-report
          path: |
            build/reports/dependency-check/
          retention-days: 7

  coverage:
    name: 📊 코드 커버리지 (Jacoco)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # main/develop push에만 실행
    if: github.event_name == 'push'

    steps:
      - name: 📥 코드 받아오기
        uses: actions/checkout@v4

      - name: ☕ JDK 17 설정
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle
          cache-dependency-path: |
            **/gradle.properties
            **/build.gradle

      - name: 🔧 Gradle wrapper 권한 설정
        run: chmod +x ./gradlew

      - name: 📝 local.properties 생성
        run: echo "sdk.dir=$ANDROID_HOME" >> local.properties

      - name: 🔥 google-services.json 생성
        run: echo '${{ secrets.GOOGLE_SERVICES_JSON }}' | base64 --decode > app/google-services.json

      - name: 🧪 단위 테스트 + 커버리지 생성
        run: |
          ./gradlew testDebugUnitTest \
            --parallel \
            --build-cache \
            --no-daemon \
            --configure-on-demand \
            -Dorg.gradle.workers.max=8 \
            -Dkotlin.incremental=true \
            -Dkotlin.caching.enabled=true || true

      - name: 📤 커버리지 리포트 업로드
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: |
            app/build/reports/coverage/debugCodeCoverage/
            app/build/test-results/testDebugUnitTest/
          retention-days: 7
