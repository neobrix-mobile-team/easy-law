name: 🔐 코드 품질 & 보안 검사

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  lint:
    name: 🔍 Android Lint 품질 검사
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 코드 받아오기
        uses: actions/checkout@v4

      - name: 🔐 Firebase 설정 (google-services.json 생성)
        env:
          G_SERVICES: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: echo "$G_SERVICES" | base64 -di > app/google-services.json

      - name: ☕ JDK 17 설정
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: 🔧 Gradle wrapper 권한 설정
        run: chmod +x ./gradlew

      - name: 🔍 Android Lint로 코드 품질 검사
        run: ./gradlew lint --info || true

      - name: 📤 Lint 결과 업로드
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lint-report
          path: |
            app/build/reports/lint-results*.html
            app/build/reports/lint-results*.xml
          retention-days: 7

  security-scan:
    name: 🔒 의존성 보안 취약점 검사 (OWASP)
    runs-on: ubuntu-latest

    steps:
      - name: 📥 코드 받아오기
        uses: actions/checkout@v4

      - name: 🔐 Firebase 설정 (google-services.json 생성)
        env:
          G_SERVICES: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: echo "$G_SERVICES" | base64 -di > app/google-services.json

      - name: ☕ JDK 17 설정
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: 🔧 Gradle wrapper 권한 설정
        run: chmod +x ./gradlew

      - name: 🔒 OWASP 보안 취약점 검사
        run: ./gradlew dependencyCheckAnalyze --info || true

      - name: 📤 보안 검사 결과 업로드
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-scan-report
          path: build/reports/dependency-check/
          retention-days: 7

  coverage:
    name: 📊 코드 커버리지 (Jacoco)
    runs-on: ubuntu-latest

    steps:
      - name: 📥 코드 받아오기
        uses: actions/checkout@v4

      - name: 🔐 Firebase 설정 (google-services.json 생성)
        env:
          G_SERVICES: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: echo "$G_SERVICES" | base64 -di > app/google-services.json

      - name: ☕ JDK 17 설정
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: 🔧 Gradle wrapper 권한 설정
        run: chmod +x ./gradlew

      - name: 🧪 단위 테스트 실행
        run: ./gradlew testDebugUnitTest --info || true

      - name: 📊 코드 커버리지 리포트 생성
        run: ./gradlew jacocoTestDebugUnitTestReport --info || true

      - name: 📤 커버리지 리포트 업로드
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: |
            app/build/reports/coverage/debugCodeCoverage/
            app/build/jacoco/
          retention-days: 7

      - name: 📊 커버리지 요약
        if: always()
        run: |
          echo "📈 코드 커버리지 목표: 70%+"
          if [ -f "app/build/reports/coverage/debugCodeCoverage/index.html" ]; then
            echo "✅ 커버리지 리포트 생성 완료"
          else
            echo "⚠️ 리포트를 찾을 수 없습니다."
          fi