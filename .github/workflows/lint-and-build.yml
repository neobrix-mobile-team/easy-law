name: 🔍 Lint & Build Check

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  lint:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
    - uses: actions/checkout@v3
    
    - name: 📦 Node.js ${{ matrix.node-version }} 설정
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    
    - name: 📥 의존성 설치 (Frontend)
      run: cd frontend && npm ci 2>/dev/null || true
    
    - name: 📥 의존성 설치 (Backend)
      run: cd backend && npm ci 2>/dev/null || true
    
    - name: 🔍 Lint 검사 (Frontend)
      run: cd frontend && npm run lint 2>/dev/null || true
    
    - name: 🔍 Lint 검사 (Backend)
      run: cd backend && npm run lint 2>/dev/null || true

  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
    - uses: actions/checkout@v3
    
    - name: 📦 Node.js ${{ matrix.node-version }} 설정
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    
    - name: 🔨 빌드 (Frontend)
      run: cd frontend && npm run build 2>/dev/null || true
    
    - name: 🔨 빌드 (Backend)
      run: cd backend && npm run build 2>/dev/null || true
