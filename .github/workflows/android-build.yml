name: 🔍 Kotlin CI (Wrapper Enforced & Stable)

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  android-ci:
    runs-on: ubuntu-latest
    timeout-minutes: 30 # 빌드 안정성을 위해 살짝 늘림

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: ☕ JDK 17 설정
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: 🔧 Gradle 실행 권한 부여
        run: chmod +x ./gradlew

      # 🔐 env와 -di 옵션을 사용하여 base64 에러 원천 차단
      - name: 🔐 Google Services JSON 생성
        env:
          G_SERVICES: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: |
          if [ -n "$G_SERVICES" ]; then
            echo "$G_SERVICES" | base64 -di > app/google-services.json
          else
            echo "⚠️ GOOGLE_SERVICES_JSON secret is missing!"
          fi

      - name: 🔐 local.properties 생성
        env:
          # GitHub Secrets에 저장한 이름을 그대로 가져옵니다.
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          LAW_API_KEY: ${{ secrets.LAW_API_KEY }}
        run: |
          # 파일이 없다면 생성하고, 있다면 내용을 추가합니다.
          touch local.properties
          # secrets-gradle-plugin이 인식할 수 있도록 local.properties에 기록
          # API Key가 비어있지 않은지 확인 후 파일에 기록
          if [ -n "GEMINI_API_KEY" ]; then
            echo "api.key=GEMINI_API_KEY" >> local.properties
          else
            echo "⚠️ GEMINI_API_KEY secret is missing!"
          fi
          
          if [ -n "LAW_API_KEY" ]; then
            echo "another.key=LAW_API_KEY" >> local.properties
          fi

      # 🧹 캐시 꼬임 방지는 필요할 때만 하는 것이 좋지만, 유지하신다면 명령어를 명확히 합니다.
      - name: 🧹 Gradle Wrapper 캐시 정리
        run: ./gradlew clean

      # 🚀 빌드 단계 (주석을 제거하고 한 줄씩 명확히 실행하거나 백슬래시를 정확히 사용)
      - name: 🚀 Unit Test & Build APK
        run: |
          ./gradlew testDebugUnitTest assembleDebug \
            --parallel \
            --build-cache \
            --no-daemon

      - name: 📤 APK 업로드
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: app/build/outputs/apk/debug/*.apk # 경로 구체화
          retention-days: 7

      - name: 📤 테스트 결과 업로드
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-results
          path: |
            app/build/reports/tests/testDebugUnitTest/
            app/build/test-results/testDebugUnitTest/